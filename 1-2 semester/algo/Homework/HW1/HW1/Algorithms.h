#pragma once
#include <time.h>
#include <iostream>
using namespace std;

void merge(int*, int, int, int);
void mergeSort(int*, int, int);
void mergeStrands(int*, int&, int*, int);
void strandSort(int*, int);